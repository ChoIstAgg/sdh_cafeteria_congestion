server {
    listen 80;
    server_name sdhcc.mooo.com;

    root /var/www/sdh_cafeteria_congestion;
    index index.html;

    location / {
        # React Router와 같은 SPA를 위해 요청된 파일이 없으면 index.html을 반환합니다.
        try_files $uri $uri/ /index.html;
    }
}

